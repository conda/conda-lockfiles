### Bug fixes

* Use the current conda YAML API when available to avoid pending deprecation warnings, while retaining the older helpers for conda 25.9. (#158)
* Restore conda 25.9 compatibility by registering plugins without unsupported metadata fields. Specifier aliases require a newer conda with alias support. Canonical format names and exporter aliases remain available on conda 25.9.
